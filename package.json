{
  "name": "ztl-coder",
  "version": "1.0.0",
  "description": "Enterprise-grade Claude Code plugin with Agent→Subagent→MCP Tool architecture. Complete software development lifecycle automation: brainstorming, planning, TDD implementation, testing, debugging, review, and deployment.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [
    "claude-code",
    "plugin",
    "workflow",
    "brainstorm",
    "plan",
    "implement",
    "review",
    "mcp"
  ],
  "author": "ztl",
  "license": "MIT",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --external playwright --external playwright-core",
    "build:watch": "bun build src/index.ts --outdir dist --target node --external playwright --external playwright-core --watch",
    "dev": "bun run --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "playwright": "^1.58.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
